### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String!
  user: User!
}

type Comment implements Reactable {
  id: ID!
  link: Link!
  reactions: [Reaction]!
  text: String!
  user: User!
}

type Link implements Reactable {
  description: String!
  id: ID!
  postedBy: User!
  reactions: [Reaction]!
  url: String!
}

type Mutation {
  createLink(description: String!, url: String!): Link!
  deleteLink(id: String!): Link!
  reactLink(id: String!, reaction: ReactionType!): Link!
  signIn(email: String!, password: String!): AuthPayload!
  signUp(email: String!, name: String!, password: String!): AuthPayload!
}

type Query {
  getFeed: [Link!]!
}

interface Reactable {
  reactions: [Reaction]!
}

type Reaction {
  comment: Comment
  id: ID!
  link: Link
  type: ReactionType!
  user: User!
}

enum ReactionType {
  ANGRY
  HAHA
  LIKE
  LOVE
  SAD
  WOW
}

type User {
  email: String!
  id: ID!
  links: [Link!]!
  name: String!
}